<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/media_route_menu_item"
        android:title="@string/media_route_menu_title"
        app:actionProviderClass="com.simplecity.amp_library.ui.views.MyMediaRouteActionProvider"
        app:showAsAction="always"/>

    <item
        android:id="@+id/menu_favorite"
        android:icon="@drawable/ic_fav"
        android:title="@string/fav_add"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_list"
        android:icon="?attr/btn_queue"
        android:title="@string/btn_queue"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_share"
        android:icon="?attr/btn_share"
        android:title="@string/share_title"
        app:showAsAction="ifRoom"/>
</menu>